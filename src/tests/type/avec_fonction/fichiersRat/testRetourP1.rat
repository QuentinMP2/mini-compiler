bool* idiotduvillage(bool* b) {
    bool baba = true;
    bool * res = &baba;
    if ((*b)) {
        return res;
    } else {
        (*res) = false;
        baba = true;
        return res;
    }
}

main {
    bool b = false;
    bool *c = idiotduvillage(&b); 
}